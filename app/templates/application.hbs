<nav style="background: #FFFFFF; opacity: 0.8;" class="navbar navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header" href="#">
            <a class="navbar-brand" href="javascript:void(0);" {{action 'goToPosts'}}>Home</a>
        </div>
        <ul class="nav navbar-nav">
            {{#if auth.authed}}
                <li>
                    <a href="javascript:void(0);" {{action 'goToNew'}}>New</a>
                </li>
                <li>
                  {{#link-to 'admin'}}Admin{{/link-to}}
                </li>
            {{/if}}
        </ul>
    </div>
</nav>
{{outlet}}
<div id="login-modal" class="modal fade" data-backdrop="static" data-keyboard="false" tabindex="-1" role="dialog" aria-labelledby="loading-label" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title" id="add-category-label">Log In</h4>
            </div>
            <div class="modal-body">
                <h3>Username</h3>
                {{input value=username type='text' class='form-control'}}
                <h3>Password</h3>
                {{input value=password type='password' class='form-control'}}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" {{action 'doLogin'}} aria-hidden="true">Log In!</button>
            </div>
        </div>
    </div>
</div>
